The website example configuration for the `file` sink (and 32 other sinks) was missing the required `encoding` field. The config example generator now correctly includes required object-type fields whose sub-options have no direct examples.

authors: pront
